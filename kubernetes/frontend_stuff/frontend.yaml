apiVersion: v1
kind: Pod
metadata:
  name: frontendApp
  namespace: projectApp
  labels:
    app: frontendApp
spec:
  containers:
  - name: frontendApp
    image: lukef2021/frontend
    ports:
    - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: frontendApp
  namespace: projectApp
  labels:
    app: frontendApp
spec:
  selector:
    app: frontendApp
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
  type: ClusterIP